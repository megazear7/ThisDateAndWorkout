#!C:/xampp/perl/bin/perl

my $run = 0;
my @array = ["apples", "bannanas", "laser beam animal monster men"];

foreach (@array){
	print "the value is: $_\n";
}

#print $array[$_] foreach 0..$#array;

#print "the variable \$run is true" if $run;

